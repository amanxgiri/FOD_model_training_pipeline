# Model registry

`registry.json` is the small, Git-tracked pointer to the active published model. Model binaries are not stored in ordinary Git history. Until a real candidate is evaluated and promoted, `active_model` remains `null`.
